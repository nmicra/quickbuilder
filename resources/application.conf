ktor {
    deployment {
        port = 8080
        port = ${?PORT}
        shutdown.url = "/buildhelper/shutdown"
    }

    application {
        modules = [com.github.nmicra.quickbuilder.ApplicationKt.appStructure]
    }

    metadata{
        reloadSeconds = 15
    }
}
